Projects done for "Introduction to programming, functional paradigm" course.
iSet module implements an interval tree with AVL balancing.
leftist module implements pirority queue as a leftist tree.
topol module implements topological sort with dfs. It uses pMap module to keep the graph.
The program gets graph as a list and puts it in a map. Then it performs topological sort.